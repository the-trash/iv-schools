- content_for :css do
  =stylesheet_link_tag 'basic/edit_form.css', :media=>'screen'
- content_for :title do
  Хранилище файлов:
  =@user.login
  |
  =t('portal_name')
.block
  %ul
    %li.system
      - text= content_tag :span, '&laquo; Назад в личный кабинет', :class=>:bar
      = link_to text, cabinet_users_url(:subdomain=>current_user.subdomain)

%h3 Хранилище файлов
.map
  %ul
    - @storage_sections.each do |section|
      %li{:style=>"padding:10px;margin:0 0 10px 0;"}
        =link_to image_tag('iconza/blue/downloads_folder.png', :style=>'vertical-align:middle;') + " " + section.name + " (#{section.storage_files.size.to_s} файлов)", storage_files_url(:id=>section.zip), :style=>'font-weight:bold;'
  
= object_errors(@page)

.edit_form
  %h5{:style=>'line-height:150%;'}
    На этапе тестирования Вам предлагается использовать единый каталог файлов,
    который называется "Основное"
    %br
    В этой папке хранятся все ваши файлы из старой версии сайта
  -#
    Создать новый раздел
      
    -form_for :storage_section, :url => storage_sections_path do |f|
      %label{:for=>:title}
        Название раздела
      %span
        = f.text_field :name, :class=>:input
        
      = submit_tag 'Создать', :class=>:submit
